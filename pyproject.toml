[project]
name = "convert2ebrl-workspace"
version = "1.0.0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "gui",
    "brf2ebrl",
    "plugins/brf2ebrl_bana",
    "plugins/brf2ebrl_nfb",
]
